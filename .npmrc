# Vercel + pnpm: hoisting para que serverless encuentre Neon/ws/Prisma adapter.
shamefully-hoist=true
# Playwright solo en e2e/ — no hoistear al root (evita next@playwright en Turbopack)
public-hoist-pattern[]=!@playwright/*
peer-dependency-rules.ignore-missing[]=@playwright/test
