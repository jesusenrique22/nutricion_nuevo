"use client";

/** Shell de la portada (`/`). El scroll es libre (sin snap forzado entre bloques). */
export function LandingLobbyShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
